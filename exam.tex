\documentclass[answers]{otagoexam}
%%%%%%%%%%%%%%%%%
% Class options %
%%%%%%%%%%%%%%%%%
%   answers - shows answers (remove it to hide answers)
%   embargoed - embargoed exam mode

%%%%%%%%%%%%%%%%%%
% Extra packages %
%%%%%%%%%%%%%%%%%%
\usepackage{graphicx}

%%%%%%%%%%%%
% Settings %
%%%%%%%%%%%%

% This command specifies the type of break between questions
% and is set to '\pagebreak', so that each question starts
% on a new page.  If you want multiple questions per page 
% (to save trees), you can change the setting to
% '\vspace{<spacing>}', such as '\vspace{3ex}' for instance,
% to have some vertical spacing between the questions. 
% You'll have to place page breaks manually.
\newcommand{\questionbreak}{\pagebreak}

% This command specifies the type of vertical spacing
% between subquestions.  The default value is '2ex', you
% can increase or decrease it as fits.
\newcommand{\subquestionspacing}{2ex}

%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Info for the title page %
%%%%%%%%%%%%%%%%%%%%%%%%%%%
\department{Department}
\papercode{Paper code}
\papertitle{Paper title}
\paperyear{2023}
\semester{Teaching period} %Semester One/Semester Two/Summer School
\timelimit{3 HOURS}


% If you don't define any of the following, defaults 
% shown here will be used 

% Instructions: layout your instructions here.  You can 
% use \showmarks{<marks>} command to show marks without
% counting them towards the total.  The \totalmarks{} 
% command gives a sum of all marks from \addmarks[<mark>]
% commands.  If you'd want auto-summed marks, place your
% manual mark value in the command below - ex.
% \totalmarks{60} to show 60 marks regardless of auto-sum.

\instructions{
  Candidates should answer \textbf{All} questions

  \vspace{0.3cm}
  Marks are shown thus: \showmarks{2}

  \vspace{0.3cm}
  The total number of marks for this exam is \totalmarks{}
}

% Material provided (default)
\providedmaterial{Nil}

% Calculators (default)
\calculators{\nocalculators} 

% Other options for calculators
%\calculators{\anymodel} %Any model without networking
%\calculators{\listA}    %List A calculators
%\calculators{\listB}    %List B calculators

% Permission for additional material (default)
\permittedmaterial{No additional material}


% Other instructions - these relate to the instructions for how to provide answers on the exam sheets: typically written answers go in the yellow book, multichoice answer onto a green sheet, but there are also options for having answers written on the exam sheet itself.  A number of templates is provided below for common scenarios (comment out the appropriate one).

% Standard exam with the short answers to go into the (yellow) answer book -- when not-embargoed, these instructions are just "Nil"
\otherinstructions[ansbook]{}


% Multichoice answers only exam
%\otherinstructions[mcq]{}

% Written answer and multichoice answers exam
%\otherinstructions[ansbook+mcq]{}

% Answers to be written on the exam sheet (or the pink setion of the exam sheet)
%\otherinstructions[onexam]{}
%\otherinstructions[onpinkexam]{}

% Instructions for when yellow answer book with the rough work is to be submitted along the answers on either: the multichoice sheet, exam sheet or the pink exam sheet answers. 
%\otherinstructions[mcq+roughwork]{}
%\otherinstructions[onexam+roughwork]{}
%\otherinstructions[onpinkexam+roughwork]{}

% Instructions for when A4 leaflets (4-page line booklets) with the rough work are to be submitted along the answers on either: the answer book, the multichoice sheet, exam sheet or the pink exam sheet answers. 
%\otherinstructions[ansbook+a4]{}
%\otherinstructions[mcq+a4]{}
%\otherinstructions[onexam+a4]{}
%\otherinstructions[onpinkexam+a4]{}

% Any additional instructions, can be inserted as text in the \otherinstructioncommand like so
%\otherinstructions[mcq]{Additional instructions}


%%%%%%%%%%%
% Content %
%%%%%%%%%%%

\begin{document}

% Title page
\maketitle


% Uncomment this if you need physical constants page 
% (can reference it with \ref{physicalconstants})
%\physicalconstants

% Uncomment this if you need formulae page (can reference 
% it with \ref{formulae})
%\formulae

% Section without a title, marked.
\section{}

To add a section to the exam use the \verb$\section{}$ command.  Sections are numbered with upper-case letters.  Exams scripts do not have to have sections.  To specify marks for the whole section use \verb$\addmarks{<mark>}$ command at the end of the section paragraph.\addmarks{12}

\begin{question}

    Create a single question within the \texttt{\textbackslash begin\{question\}}...\texttt{\textbackslash end\{question\}} environment.  Questions are numbered with Arabic numerals.

    \begin{subquestions}
        \item List sub-questions using the \texttt{subquestions} environment within the question text.
        \item Sub-questions are numbered with lower-case letters.

        \item{
            \begin{subquestions}
                \item Nesting \texttt{subquestion} environments is fine.
                \item Second level sub-questions are numbered with Roman numerals.
            \end{subquestions}
        }
        
    \end{subquestions}


\end{question}



% Section with a title
\section{Title}

To create a section with a title use the \verb$\section{<title text>}$ command.

% Marked question
\begin{question}

    Question numbering continues regardless of the sections.  To specify marks for the entire question use the \texttt{\textbackslash addmarks\{<mark>\}} command at the end of the question paragraph.\addmarks{6}

    \begin{subquestions}
        \item List sub-questions using the \texttt{subquestions} environment environment within the question text.
        \item Sub-questions are numbered with lower-case letters.

        \item{
            \begin{subquestions}
                \item Nesting \texttt{subquestion} environments is fine.
                \item Second level sub-questions are numbered with Roman numerals.
            \end{subquestions}
        }
        
    \end{subquestions}
\end{question}

% Marked sub-questions
\begin{question}
    
    By default, each question starts on a new page.  To disable auto-page change the\\ \texttt{\textbackslash questionbreak\{\}} setting.  To specify marks per sub-question use\\ \verb$\addmarks{<mark>}$ command at the end of the respective \texttt{item} text.
    

    \begin{subquestions}
        \item List sub-questions using the \texttt{subquestions} environment within the question text. \addmarks{3}
        \item Sub-questions are numbered with lower-case letters. \addmarks{2}

        \item To change the vertical spacing between sub-question items change the\\ \texttt{\textbackslash subquestionspacing\{\}} setting. \addmarks{2}
        \item{
            \begin{subquestions}
                \item Nesting \texttt{subquestion} environments is fine. \addmarks{4}
                \item Second level sub-questions are numbered with Roman numerals. \addmarks{1}
            \end{subquestions}
        }
        
    \end{subquestions}

\end{question}

% Question with answers
\begin{question}

    To include answers use the \texttt{\textbackslash answer\{<answer text>\} command} within a question or under a sub-question.  Answers are printed in red and they show up on the pdf only when \texttt{answers} option is passed to the \texttt{otagoexam} document class.

    \begin{subquestions}
        \item Sub-question \addmarks{3}

        \answer{You can provide answer for each sub-question.
            \begin{itemize}
                \item To include a mark break-down for an answer, use the \texttt{\textbackslash showMarks\{<marks>\} command} \showmarks{1.5} 
                \item The marks in \texttt{\textbackslash showMarks\{<marks>\} command} do not affect the marks total. \showmarks{1.5} 
            \end{itemize}
        }

        \item Another sub-question \addmarks{2}
    \end{subquestions}

    \answer{To add an answer at the end of the question, make sure its within the question body.}

    \answer{All answers are removed in the (no \texttt{answers} option) \texttt{otagoexam} document class mode.}

\end{question}

% This command adds the "please do not turn over"
% page at the end inserting a blank page (if
% needed) so that it always lands on an 
% even page at the back of the exam.
\turnoverpage


\end{document}
